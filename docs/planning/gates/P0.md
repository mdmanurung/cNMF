# Gate P0 — unchanged baseline + trustworthy evaluation

Date: 2026-09-19  
Protocol: v1.0.1, sha256 `715639895663bc74e7b864bc1cfae60c2fcd05b013e25f23016d45e86f4f9ca5`  
Code: harness `9ef15dc`, upstream `5dbc5baaa0b9079b55bce554d801caa235a50457` (`src/cnmf/**` unmodified, verified empty diff)  
Environment: `cnmf_bench` (Python 3.10.20, cnmf 1.7.1 editable); R comparator in `R4_51` (R 4.5.1, GeneNMF 0.9.6 @ `59942b2`)

## Experiments and eligible units

Tracked evidence: `docs/benchmarks/RESULTS.tsv` 4430 rows / `EXPERIMENTS.tsv`
136 experiments (SMOKE 40, DEVELOPMENT 96), all `status: ok_provisional`,
`allow_scientific_promotion: false`. Metrics present: prediction (training
scale + count back-transform + null) 1242 rows each; `program_recovery_cosine_v1`
and `usage_error_v1` 80 rows each with the matched null beside them in
`EXPERIMENTS.notes` (80/136 experiments); cost triad + `failed_fits_v1` on all
136. Independent unit: donor (§3.5); cells never treated as independent.

## Primary paired effects (metric report, not adoption)

- Donor-blocking (D008): blocked-vs-random CV gap +2.92%, t = 3.11, n = 48 —
  real; mechanism is donor count (t = +4.30), not per-donor leakage (t = −0.37).
- DEVELOPMENT feasibility (D012): NO-GO on mis-specified criterion 2; loss and
  silhouette curves nevertheless minimise at K_true in both folds;
  under-convergence ruled out (max_iter 1000 diagnostic). Replacement
  criterion 2′ pre-registered (seed 31337), not yet run.
- Kang 2018 diagnostic (D029): baseline cNMF recovers the interferon program,
  AUC 0.914–0.948 at every rank, 16/30 pre-listed ISGs — pipeline works on real
  input; configuration 000 only, promotes nothing.
- Recovery peaks at K_true in every fold/configuration (P0-04).

## Safeguards

Corruption battery green (3 invariances to 1e-12, 4 penalties); leakage audit
green (dictionary, preprocessing, inference usages, cache identities invariant
to held-out perturbation; row-total normalisation explicitly tested); pooling
guard + cross-arm panel audit enforced; D004 tolerance CONFIRMED (400× above
noise, 360× below mildest structural corruption). Upstream suite: 37/38 pass
with the diagnosed scale-blind failure carried as B004, not worked around.

## Costs and failures

Every experiment carries wall/cpu/peak-memory + `failed_fits_v1` (always
emitted, including 0). Fit-scope cost attributed once per fold; silhouette
sweep folded into fit-scope (D024). No tracked row regenerated this session.

## Deviations

D003 (P0-01 before S0-03), D006 (simulator before schemas), D014 (`variant`),
D018/D021/D026 (work proceeds around P0-02, now closed by P0-02 DONE),
D025 (`inner_split_id`), D030→D031 (P0-09 block lifted via R4_51). All
recorded, none silent.

## Fence scope (D032)

Three entries remain, none P0-hardenable: `splits.outer_donor_folds` (P0-06 —
its un-fencing condition is the v1.1 amendment + first A-ON run, i.e. P1-01
business); `features.discovery_sample_b` (P2-01); `features.consensus_c`
(P3-01). The P0 gate is therefore scoped to P0-owned hardening, which is
complete (`skeleton.run` un-fenced at D027 with cache/restart/workflow/SLURM
evidence). The remaining entries transfer to their owning gates; all rows they
produced stay `ok_provisional`, correctly, and no adoption decision cites them.

## Decisions

- Software status: **PASS** (smoke + development; SLURM profile recorded
  NOT_RUN_ON_SLURM).
- Scientific adoption: **NOT_APPLICABLE** — P0 changes no algorithm. Nothing
  is promoted; `smoke_can_promote_feature: false` stands.

## Supported / unsupported scope

Supported: the harness reproduces upstream within D004, measures prediction /
recovery / usage / cost with stated units, refuses leakage/pooling/panel
violations, and restarts cleanly from cache. Unsupported: any feature claim
(A/B/C untested against baselines), biological generalisation (Kang is a
pipeline diagnostic, not confirmation), GeneNMF comparability (no gene-set
metric until P2-01; no comparator rows).

## Next permitted task

P1-01: the v1.1 amendment (seven batched items per the warm-tome plan §7:
`delta`, precision/recall threshold, ambiguous threshold, D009 vocabularies,
criterion-was-wrong branch, nested-fold + `inner_split_id` section,
primary-loss conflict) + frozen A margins + criterion-2′ run. P0-06 closes with
that amendment, not before.
