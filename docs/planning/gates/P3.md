# Gate P3 — feature C (one contribution per run per consensus program)

Date: 2026-09-19  
Protocol: v1.1, sha256 `cc5241076a6c3e5f98b7575f0b09b12337c119e6ddaea685514692ebc9d42ec9`  
Code: harness P3-02/03 (D042), upstream `5dbc5baaa0b9079b55bce554d801caa235a50457` (unmodified)  
Comparison: `000`v`001` + `010`v`011`, fixed K, identical factor banks (84/84
pairs hash-equal); 3 scenarios at DEVELOPMENT. Full table:
`registry/p3-04_c_comparisons.tsv`.

## Endpoints vs frozen margins (contracts/C.md)

- Primary (training-scale prediction): C never improves anywhere — ON ≥ OFF
  in 12/12 (fold × rank × scenario) cells above K_true, deltas +0.0 to +2.8
  vs margin 30. No gain, including in `C_duplicate_merge` (deltas ≤ +0.8),
  the regime built for C to help.
- No-op at/below K_true: C-ON == C-OFF bitwise on every endpoint — the
  structural claim holds, and it bounds the harm: where C cannot fire it
  cannot hurt.
- Recovery harm: ≤0.0015 everywhere (margin 0.01). Usage harm: ≤0.034
  (margin 0.05). Cost: 0.96–1.04× same-session (cap 5×). All pass, none
  approached.
- Provenance: retained/omitted counts recorded per (fold, K); no silent
  replacements; NaN banks refuse.

## Decisions

- Software status: **PASS** (OFF-path pinned bitwise to upstream on a real
  bank; no-op and incomplete-run properties tested; identical-bank assertion
  holds on 84 real pairs).
- Scientific adoption: **DROP** — no worthwhile benefit in any tested regime
  including the duplicate-merging target, with small consistent harm-direction
  above K_true. Default (upstream median over all factors) remains. Scope:
  DEVELOPMENT simulation, fixed rank, centroid-nearest rule. The mean-vote
  alternative stays deferred, not a rescue.

## Supported / unsupported scope

Supported: C is a safe no-op where the constraint holds and a small net
negative where it fires, at DEVELOPMENT scale; the tie-break rule is frozen
and its effect measured. Unsupported: 100/101 + 110/111 (omitted — would
measure dropped-A harm); selected-rank C (no operative selector); real-data
statements.

## Next permitted task

M1-01: full factorial + anchors + interaction report — with A/B/C all dropped
at DEVELOPMENT, the factorial's role is confirming no interaction reverses a
verdict, not discovering a winner (see M1 plan note in PROGRESS).
Fence: EMPTY — `features.consensus_c` un-fenced here (D043); `registry_is_empty()`
is true for the first time this programme.
